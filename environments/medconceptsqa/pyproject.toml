[project]
name = "medconceptsqa"
description = "Your environment description here"
tags = ["placeholder-tag", "train", "eval"]
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "verifiers>=0.1.5.post0",
    "datasets>=4.1.1",
]

[tool.prime.environment]
loader = "medconceptsqa:load_environment"
display_name = "MedConcepts QA"
visibility = "PUBLIC"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
